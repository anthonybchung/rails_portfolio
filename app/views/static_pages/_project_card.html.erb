<div class="w-40 h-full mx-2 inline-block bg-red-500 " id= <%= index%>>
	<p><%= project.name %></p>
	<p><%= project.description %></p>
	<%= link_to "more details ...", "/projects/#{project.id}" %>
</div>
